*showtime.txt*             Treesitter-powered reference highlighter for Neovim
                                   For NVIM v0.8.0    Last change: 2026 May 06

==============================================================================
Table of Contents                                 *showtime-table-of-contents*

1. showtime.nvim                                      |showtime-showtime.nvim|
  - Features                                 |showtime-showtime.nvim-features|
  - Requirements                         |showtime-showtime.nvim-requirements|
  - Installation                         |showtime-showtime.nvim-installation|
  - Configuration                       |showtime-showtime.nvim-configuration|
  - Commands                                 |showtime-showtime.nvim-commands|
  - How It Works                         |showtime-showtime.nvim-how-it-works|
  - Health Check                         |showtime-showtime.nvim-health-check|
  - License                                   |showtime-showtime.nvim-license|

==============================================================================
1. showtime.nvim                                      *showtime-showtime.nvim*

Treesitter-powered reference highlighter for Neovim 0.11+. When your cursor
sits on an identifier, every other reference to that identifier within its
containing scope lights up. No external dependencies. No per-language
configuration.


FEATURES                                     *showtime-showtime.nvim-features*

- **Scope-aware highlighting**: References are found within the containing function, method, class, or module — not the entire file.
- **Treesitter-native**: Works with any language that has a treesitter parser. No per-language setup.
- **Fast**: Viewport pruning, per-window caching, and iterative DFS make highlighting imperceptible even on large files.
- **Zero config**: Works out of the box with sensible defaults. `setup()` is optional.


REQUIREMENTS                             *showtime-showtime.nvim-requirements*

- Neovim >= 0.11
- A treesitter parser for the language you’re editing (`:TSInstall <lang>`)


INSTALLATION                             *showtime-showtime.nvim-installation*

Using lazy.nvim <https://github.com/folke/lazy.nvim>:

>lua
    {
        'claydugo/showtime.nvim',
        event = 'VeryLazy',
        opts = {},
    }
<

Or with explicit config:

>lua
    {
        'claydugo/showtime.nvim',
        event = 'VeryLazy',
        config = function()
            require('showtime').setup({
                delay = 0,
                max_matches = 500,
            })
        end,
    }
<


  [!NOTE] Calling `setup()` is optional. The plugin works with defaults as soon
  as it loads.

CONFIGURATION                           *showtime-showtime.nvim-configuration*

All options and their defaults:

>lua
    require('showtime').setup({
        --- Milliseconds before highlighting (0 = immediate).
        delay = 0,
        --- Highlight group for references.
        hl_group = 'ShowtimeReference',
        --- Safety cap on extmarks per update cycle.
        max_matches = 500,
        --- Minimum references required before highlighting activates.
        --- The cursor's own occurrence counts, so the default of 2 means
        --- "highlight only when there is at least one other occurrence."
        --- An identifier with no siblings stays unhighlighted.
        min_matches = 2,
        --- Treesitter languages to skip (e.g., { 'markdown', 'help' }).
        exclude_languages = {},
        --- Buffer types to skip.
        exclude_buftypes = { 'nofile', 'terminal', 'prompt' },
        --- Per-language scope node overrides (deep-merged with builtins).
        --- See "Adding a Language" below.
        scope_nodes = nil,
    })
<


ADDING A LANGUAGE ~

The plugin ships with scope tables for Lua, Python, JavaScript/TypeScript/TSX,
Go, Rust, C, C++, and Ruby. For any other language with a treesitter parser, a
generic fallback kicks in (matching node types containing `_definition`,
`_declaration`, `module`, etc.) — usually good enough.

When the fallback misses or you want different scope boundaries, set
`scope_nodes`. The table is deep-merged with builtins, so you only need to
specify what you’re adding or overriding.

**Adding an unsupported language** — list every node type that should act as
a scope boundary. Use `:InspectTree` on a sample file to find them.

>lua
    require('showtime').setup({
        scope_nodes = {
            bash = {
                program = true,
                function_definition = true,
                subshell = true,
            },
        },
    })
<

**Extending a builtin language** — keys you add merge with the existing set.
Here, Lua keeps its `chunk` / `function_definition` / `do_block` defaults _and_
now treats `for_statement` as a scope boundary:

>lua
    require('showtime').setup({
        scope_nodes = {
            lua = {
                for_statement = true,
            },
        },
    })
<


HIGHLIGHT GROUPS ~

  -----------------------------------------------------------------------
  Group                   Default                 Description
  ----------------------- ----------------------- -----------------------
  ShowtimeReference       links to                Applied to all matching
                          LspReferenceText        references (except the
                                                  one under cursor)

  -----------------------------------------------------------------------
Override in your colorscheme or config:

>lua
    vim.api.nvim_set_hl(0, 'ShowtimeReference', { bg = '#2a2a3a' })
<


PER-BUFFER DISABLE ~

Disable showtime for a specific buffer:

>lua
    vim.b.showtime_disabled = true
<


COMMANDS                                     *showtime-showtime.nvim-commands*

  Command            Description
  ------------------ --------------------------------
  :ShowtimeEnable    Enable reference highlighting
  :ShowtimeDisable   Disable reference highlighting
  :ShowtimeToggle    Toggle reference highlighting

HOW IT WORKS                             *showtime-showtime.nvim-how-it-works*

1. On `CursorMoved`, the engine checks if the cursor is on a treesitter identifier (leaf node whose type contains "identifier" or "name").
2. It walks up the tree to find the nearest scope boundary using per-language node type tables (with a generic fallback for unknown languages). Falls back to the tree root.
3. An iterative DFS traverses the scope, collecting all nodes with the same type and text — but only within the visible viewport.
4. Matching nodes get highlighted via extmarks. The node under the cursor is excluded.
5. A per-window cache keyed on buffer, changedtick, node identity, cursor position, scope range, and viewport bounds skips redundant work.


HEALTH CHECK                             *showtime-showtime.nvim-health-check*

Run `:checkhealth showtime` to verify your setup.


LICENSE                                       *showtime-showtime.nvim-license*

MIT — see LICENSE <LICENSE>.

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
